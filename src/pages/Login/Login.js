import React from "react";
import { ToastContainer, toast } from "react-toastify";
const Login = () => {
  return (
    <div>
      login
      <ToastContainer />
    </div>
  );
};

export default Login;
